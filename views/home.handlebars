{{!-- Extend the main layout --}}
{{#extend "main"}}

{{#content "body"}}
<h1>Welcome to the Blog</h1>

{{#each posts as |post|}}
    <div class="post">
        <h2>{{post.title}}</h2>
        <p>{{post.body}}</p>
    </div>
{{/each}}
{{/content}}

{{/extend}}
